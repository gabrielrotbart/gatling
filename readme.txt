This tool uses historic screenshots (taken from a build that had screenshots turned on)
 and compares them to another set of screenshots, showing the differences -> visual regressions.

sudo apt-get install imagemagick
sudo apt-get install libmagick9-dev
sudo apt-get install ruby1.8-dev
sudo -E gem install rmagick